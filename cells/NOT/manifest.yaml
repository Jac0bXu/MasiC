name: NOT
yosys_types: ["$_NOT_"]
family: combinational
# REPLACE the footprint and coords below with what you actually built.
# The values here are placeholders sized for the canonical "torch on block" inverter.
footprint: [1, 1, 3]
ports:
  inputs:
    A: {coord: [0, 0, 0], kind: lever}
  outputs:
    Y: {coord: [0, 0, 2], kind: lamp}
delay_ticks: 1
truth_table:
  - {A: 0, Y: 1}
  - {A: 1, Y: 0}
